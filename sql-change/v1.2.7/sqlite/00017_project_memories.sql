-- +goose Up
-- Deprecated: SQLite-backed project memory storage was replaced by Markdown files.
-- Kept as a no-op migration so existing goose version ordering remains stable.

-- +goose Down
-- no-op
